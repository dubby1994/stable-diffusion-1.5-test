# Stable Diffusion 1.5 图生图 Web 服务依赖
# Python >= 3.10

# Web 框架
flask>=3.1.3

# 图像处理
Pillow>=12.2.0

# 模型下载（阿里魔搭社区，国内服务器稳定）
modelscope>=1.37.1

# PyTorch（必须 cu128+ 才支持 RTX 5070 Ti 的 sm_120 架构）
# 不要直接 pip install torch 从 PyPI（PyPI 默认是 CPU 版）
# 正确安装方式见 README "重装 torch" 部分
torch>=2.9.1

# Stable Diffusion 推理
diffusers>=0.38.0
transformers>=5.12.1
accelerate>=1.14.0

# 注意：torchvision / torchaudio 已卸载
# torch 升级到 2.9.1 后旧版 torchvision/torchaudio 不兼容，
# transformers 会自动 fallback 到 PIL 后端，不影响 SD 推理
